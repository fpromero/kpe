# Notebooks

Use this directory only for exploration and visual analysis. Number notebooks (`01_`, `02_`, and so on), document their inputs and outputs, and move all reusable logic to `src/ftm_kpe/`.
